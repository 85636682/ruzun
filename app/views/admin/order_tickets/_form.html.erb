<%= form_for [:admin, @order_ticket], html: { class: "form-horizontal" } do |f| %>
    <div class="form-group">
        <label for="inputEmail3" class="col-sm-2 control-label">是否使用</label>
        <div class="col-sm-10">
            <label class="radio-inline">
                <%= f.check_box :used %>
            </label>
        </div>
    </div>
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <button type="submit" class="btn btn-default">保存</button>
        </div>
    </div>
<% end %>